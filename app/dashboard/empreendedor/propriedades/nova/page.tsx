"use client"
import { useState } from "react"; import { useRouter } from "next/navigation"; import Link from "next/link"; import { ArrowLeft, Save } from "lucide-react"; import { Header } from "@/components/header"; import { Footer } from "@/components/footer"; import { Card,CardContent,CardHeader,CardTitle } from "@/components/ui/card"; import { Input } from "@/components/ui/input"; import { Textarea } from "@/components/ui/textarea"; import { Label } from "@/components/ui/label"; import { Button } from "@/components/ui/button"; import { Select,SelectContent,SelectItem,SelectTrigger,SelectValue } from "@/components/ui/select"; import { useAuth } from "@/contexts/auth-context"; import { crudPropriedade } from "@/lib/client-store"
export default function Page(){const router=useRouter();const {user}=useAuth();const [f,setF]=useState({nome:"",tipo:"sitio",preco:"",capacidade:"",endereco:"",cidade:"",estado:"SP",descricao:""});const set=(k:string,v:string)=>setF(x=>({...x,[k]:v}));const submit=(e:React.FormEvent)=>{e.preventDefault();if(!user)return;crudPropriedade(null,{...f,preco:Number(f.preco),capacidade:Number(f.capacidade),tipo:f.tipo as any,imagens:["/placeholder.jpg"],comodidades:[]},user.id);router.push("/dashboard/empreendedor/propriedades")};return <div className="min-h-screen flex flex-col"><Header/><main className="flex-1 py-8"><div className="container mx-auto px-4 max-w-3xl"><Link href="/dashboard/empreendedor/propriedades" className="inline-flex gap-2 mb-6 text-muted-foreground"><ArrowLeft className="h-4 w-4"/>Voltar</Link><Card><CardHeader><CardTitle>Nova Propriedade</CardTitle></CardHeader><CardContent><form onSubmit={submit} className="space-y-5"><div><Label>Nome *</Label><Input required value={f.nome} onChange={e=>set("nome",e.target.value)}/></div><div className="grid md:grid-cols-2 gap-4"><div><Label>Tipo</Label><Select value={f.tipo} onValueChange={v=>set("tipo",v)}><SelectTrigger><SelectValue/></SelectTrigger><SelectContent><SelectItem value="fazenda">Fazenda</SelectItem><SelectItem value="sitio">Sítio</SelectItem><SelectItem value="chacara">Chácara</SelectItem><SelectItem value="pousada">Pousada</SelectItem><SelectItem value="camping">Camping</SelectItem></SelectContent></Select></div><div><Label>Preço por noite</Label><Input type="number" min="0" value={f.preco} onChange={e=>set("preco",e.target.value)}/></div><div><Label>Capacidade</Label><Input type="number" min="1" value={f.capacidade} onChange={e=>set("capacidade",e.target.value)}/></div><div><Label>Estado</Label><Input value={f.estado} onChange={e=>set("estado",e.target.value)}/></div></div><div><Label>Endereço</Label><Input value={f.endereco} onChange={e=>set("endereco",e.target.value)}/></div><div><Label>Cidade</Label><Input value={f.cidade} onChange={e=>set("cidade",e.target.value)}/></div><div><Label>Descrição</Label><Textarea rows={5} value={f.descricao} onChange={e=>set("descricao",e.target.value)}/></div><Button type="submit"><Save className="mr-2 h-4 w-4"/>Cadastrar</Button></form></CardContent></Card></div></main><Footer/></div>}
