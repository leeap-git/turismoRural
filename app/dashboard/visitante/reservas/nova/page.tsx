"use client"
import{useState}from"react";import{useRouter}from"next/navigation";import{Header}from"@/components/header";import{Footer}from"@/components/footer";import{Card,CardContent,CardHeader,CardTitle}from"@/components/ui/card";import{Input}from"@/components/ui/input";import{Label}from"@/components/ui/label";import{Button}from"@/components/ui/button";import{Select,SelectContent,SelectItem,SelectTrigger,SelectValue}from"@/components/ui/select";import{useAuth}from"@/contexts/auth-context";import{crudReserva,loadStore}from"@/lib/client-store"
export default function Page(){const{user}=useAuth();const router=useRouter();const s=loadStore();const props=s.propriedades.filter(p=>p.ativo);const[f,setF]=useState({propriedadeId:props[0]?.id||"",dataInicio:"",dataFim:"",pessoas:"1",valorTotal:""});const set=(k:string,v:string)=>setF(x=>({...x,[k]:v}));const save=(e:React.FormEvent)=>{e.preventDefault();if(!user)return;const p=props.find(x=>x.id===f.propriedadeId);crudReserva(null,{...f,pessoas:Number(f.pessoas),valorTotal:Number(f.valorTotal||p?.preco||0),status:"pendente"},user.id);router.push("/dashboard/visitante")};return <div className="min-h-screen flex flex-col"><Header/><main className="flex-1 py-8"><div className="max-w-2xl mx-auto px-4"><Card><CardHeader><CardTitle>Nova Reserva</CardTitle></CardHeader><CardContent><form className="space-y-4" onSubmit={save}><div><Label>Propriedade</Label><Select value={f.propriedadeId} onValueChange={v=>set("propriedadeId",v)}><SelectTrigger><SelectValue/></SelectTrigger><SelectContent>{props.map(p=><SelectItem key={p.id} value={p.id}>{p.nome} — R$ {p.preco}</SelectItem>)}</SelectContent></Select></div><div className="grid md:grid-cols-3 gap-4"><div><Label>Entrada</Label><Input type="date" required value={f.dataInicio} onChange={e=>set("dataInicio",e.target.value)}/></div><div><Label>Saída</Label><Input type="date" value={f.dataFim} onChange={e=>set("dataFim",e.target.value)}/></div><div><Label>Pessoas</Label><Input type="number" min="1" value={f.pessoas} onChange={e=>set("pessoas",e.target.value)}/></div></div><div><Label>Valor total</Label><Input type="number" min="0" value={f.valorTotal} onChange={e=>set("valorTotal",e.target.value)}/></div><Button type="submit">Criar reserva</Button></form></CardContent></Card></div></main><Footer/></div>}
