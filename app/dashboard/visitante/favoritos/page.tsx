"use client"
import{useEffect,useState}from"react";import{Header}from"@/components/header";import{Footer}from"@/components/footer";import{Card,CardContent,CardHeader,CardTitle}from"@/components/ui/card";import{Button}from"@/components/ui/button";import{useAuth}from"@/contexts/auth-context";import{loadStore,toggleFavorito}from"@/lib/client-store"
export default function Page(){const{user}=useAuth();const[data,setData]=useState(loadStore().favoritos);const refresh=()=>setData(loadStore().favoritos);useEffect(()=>{window.addEventListener("turismo-rural-store",refresh);return()=>window.removeEventListener("turismo-rural-store",refresh)},[]);const props=loadStore().propriedades;const mine=data.filter(f=>f.usuarioId===user?.id);return <div className="min-h-screen flex flex-col"><Header/><main className="flex-1 py-8"><div className="container mx-auto px-4"><h1 className="text-3xl font-serif font-bold mb-6">Meus Favoritos</h1><div className="grid md:grid-cols-2 lg:grid-cols-3 gap-4">{mine.map(f=>{const p=props.find(x=>x.id===f.propriedadeId);if(!p)return null;return <Card key={f.id}><img src={p.imagens[0]||"/placeholder.jpg"} className="h-40 w-full object-cover"/><CardHeader><CardTitle>{p.nome}</CardTitle></CardHeader><CardContent><p className="text-sm text-muted-foreground mb-3">{p.cidade}, {p.estado}</p><Button variant="outline" onClick={()=>user&&toggleFavorito(user.id,p.id)}>Remover dos favoritos</Button></CardContent></Card>})}{!mine.length&&<Card><CardContent className="py-12 text-center text-muted-foreground">Nenhum favorito ainda.</CardContent></Card>}</div></div></main><Footer/></div>}
